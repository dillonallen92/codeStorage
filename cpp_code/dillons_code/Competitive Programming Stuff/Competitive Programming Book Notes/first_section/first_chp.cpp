#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

// examples of typedef in competitive programming
typedef vector<int> vi;
typedef pair<int, int> pi;
typedef long long ll;

// macros that are suggested in the book
#define F first
#define S second
#define PB push_back
#define MP make_pair

// there is even a for loop macro that is used
#define REP(i, a, b) for(int i = a; i <= b; i++)


int main()
{
    // Messing with inputs and outputs in C++ again
    /*
    string s;
    cout << "Enter your name: "; 
    cin >> s;
    cout << "Your name is " << s << "\n";
    */

   // running code to try the example of REP
   REP(i,0,10)
   {
       cout << i << "\n";
   }

   // personally I like the for loop implementation a lot better
   // but I know this is the way to shorten all your code
   


}

